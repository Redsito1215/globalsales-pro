# Tareas — MongoDB / ELT

- [x] Estructura `backend/`, `data/`, `db/`
- [x] Docker Compose con servicio `mongo`
- [x] Perfil `etl-mongo` (CSV → Parquet → MongoDB)
- [ ] Ejecutar ELT en Docker: `docker compose --profile etl-mongo run --rm etl-mongo`
- [ ] Verificar colecciones en MongoDB Compass
- [ ] Probar API: `http://localhost:8000/docs`
- [x] Obsoleto movido a `eliminar/` (PocketBase, `files/`, etc.)
