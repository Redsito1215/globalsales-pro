# Variables opcionales para Airflow (mismo docker-compose.yml).
# Puedes copiar a .env en la raíz del repo o a airflow/.env y usar:
#   docker compose --env-file airflow/.env --profile airflow up -d

AIRFLOW_UID=50000
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin
